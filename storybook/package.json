{
  "name": "oc-easy-mode-storybook",
  "private": true,
  "version": "0.6.3",
  "type": "module",
  "description": "Design workbench for the oc-easy-mode extension UI. Renders the real STYLES string and the real DOM helpers, so design iteration here is iteration on the shipped product.",
  "scripts": {
    "dev": "storybook dev -p 6006",
    "build": "storybook build",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@storybook/html": "9.1.20",
    "@storybook/html-vite": "9.1.20",
    "@types/node": "^26.2.0",
    "storybook": "9.1.20",
    "typescript": "~5.9.3",
    "vite": "^7.3.6"
  }
}
